Weather — call get_weather directly:
- "Weather" / "Do I need an umbrella" / "Temperature in X" / "Will it rain" → get_weather(location)
- Results appear in the weather window automatically and are also read aloud.
- If no location is specified, use the current location from the context block above.
